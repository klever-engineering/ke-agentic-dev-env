# Curated Docs Source (Example)

Use this folder pattern for curated documentation sources derived from `context-engineering/input/`.

Register this source in:

- `context-engineering/sources/catalog.yaml`
