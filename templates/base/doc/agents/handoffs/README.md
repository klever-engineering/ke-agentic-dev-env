# Multi-Agent Handoffs

Use `TEMPLATE.md` for agent-to-agent and agent-to-human transitions.

Create handoffs when:

- execution crosses sessions
- HITL approval is required
- blockers prevent completion in current run

Every handoff should include exact next command/action and validation state.
