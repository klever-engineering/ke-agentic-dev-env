name: Secret Scanning

on:
  pull_request:
  push:
    branches: [main]

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run local secret scan policy
        run: |
          chmod +x scripts/secret-scan.sh
          ./scripts/secret-scan.sh
