# Sources

Store curated and agent-consumable context here.

For advanced context operations, maintain source registry metadata in:

- `context-engineering/sources/catalog.yaml` (available in `context-ops` profile)

Keep source names stable and avoid duplicate canonical artifacts.
